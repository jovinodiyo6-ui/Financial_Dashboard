services:
  - type: web
    name: financial-dashboard-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python "Financial dashboard back end.py"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        scope: run
